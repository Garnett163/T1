import { render } from '@testing-library/react';
import QualityPromo from './QualityPromo';
import React from 'react';

describe('QualityPromo', () => {
  it('QualityPromo Should render correctly', () => {
    render(<QualityPromo />);
  });
});
